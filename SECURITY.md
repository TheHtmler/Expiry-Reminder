# 安全说明

## 敏感信息

以下内容不得提交到 Git、写入小程序客户端或发送到公开协作渠道：

- 微信小程序 AppSecret
- 微信代码上传私钥
- CloudBase 或腾讯云访问密钥
- 腾讯云 OCR SecretId 与 SecretKey
- 微信 access token
- 用户凭证图片的长期可访问地址

仓库只提供 `.env.example` 中的变量名称，不保存真实值。

## 数据隔离

家庭数据以 `householdId` 为租户边界。服务端必须验证当前用户是目标家庭的有效成员，
并在数据库查询与更新条件中包含 `householdId`。管理员操作还需要验证角色。

商品图片、发票、保修凭证和 OCR 临时图片使用家庭隔离路径。临时访问地址只能在
完成权限校验后生成。

## 日志

日志允许记录请求 ID、错误码、目标资源 ID 和时间，不得记录：

- 密钥或 token
- 完整临时文件 URL
- OCR 原始凭证图片
- 其他家庭成员的个人资料

## 漏洞处理

发现越权、密钥泄漏、重复消息、大量误提醒或数据不可恢复问题时，应停止发布并保留
现场信息。修复必须包含回归测试，未经验证不得直接发布到 `main`。
