<view class="stepper">
  <view class="step {{quantity <= 0 ? 'disabled' : ''}}" hover-class="btn-hover" bindtap="decrease" aria-label="减少数量">−</view>
  <text class="quantity">{{quantity}}</text>
  <view class="step" hover-class="btn-hover" bindtap="increase" aria-label="增加数量">+</view>
</view>
